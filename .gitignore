# ProRes / source masters — too large for GitHub (>100 MB), not needed on the web
material/*.mov

# macOS
.DS_Store
**/.DS_Store

# Editor-Einstellungen & alte Seitenkopie
.vscode/

# Interne Notizen, gehören nicht ins öffentliche Repo
Plan.txt
styleguide.txt
google_ads.txt
